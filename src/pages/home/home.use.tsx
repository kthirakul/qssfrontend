import { Services } from '../../app.services'

const HomeUse = () => {
  const { number, route } = Services()

  // state
  // declare
  // function
  // execute

  return { number, route }
}

export default HomeUse
