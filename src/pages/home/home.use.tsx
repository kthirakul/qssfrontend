import { Services } from '../../app.services'

const HomeUse = () => {
  const { number } = Services()

  // state
  // declare
  // function
  // execute

  return { number }
}

export default HomeUse
